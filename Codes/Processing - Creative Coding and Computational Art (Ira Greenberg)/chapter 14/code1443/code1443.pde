private void setLighting(Triangle3D t){    // get light ratio    float intensity = getLightIntensity(t);    // get RGB color components from current fill color    float r = p.red(currentFillColor);    float g = p.green(currentFillColor);    float b = p.blue(currentFillColor);    // update color components    r*=intensity;    g*=intensity;    b*=intensity;    r+=ambientLt[0];    g+=ambientLt[1];    b+=ambientLt[2];    // reset fill with updated color values    p.fill(p.color(r, g, b));  }